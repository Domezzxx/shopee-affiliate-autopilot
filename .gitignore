# secrets / config
.env

# data + generated media (ภาพ/วีดีโอที่ Gemini สร้าง + sqlite)
data/
adb/
samples/

# python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# n8n local state
n8n/.n8n/

# cloudflared (binary โหลดเอง + log) สำหรับ public URL ของ IG
scripts/cloudflared.exe
scripts/*.log

# ไฟล์วีดีโอ/สื่อทดสอบใน scripts (binary ไม่ควรเข้า git)
scripts/*.mp4
scripts/*.png

# Wav2Lip (clone + โมเดล ~500MB) สำหรับ AI persona lip-sync — โหลดเองด้วย scripts/setup_persona.ps1
tools/

# editor / os
.idea/
.vscode/
.DS_Store
Thumbs.db

.vercel

# Claude Code local config (preview/launch)
.claude/

.env*
